node_modules/
.env
.env.local
.env*.local

# Web
web/.next/
web/out/
*.tsbuildinfo

# Flutter
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/android/.gradle/
app/android/local.properties
app/windows/flutter/ephemeral/

# Generated brand rasters are reproducible: node brand/build-assets.mjs
brand/png/

# Ad-hoc screenshots and design references dropped in the repo root.
Screenshot*.png
error.jpeg
logo.png
logo\ reffrances.png
ui.png

# Packaged desktop artifacts (built by app/windows/installer/accounic.iss).
app/build/installer/

# Data-safety snapshots: they hold real person ids and real balances, so they
# are evidence for a migration, not repository contents (db/tools/snapshot.mjs).
db/tools/snapshot-*.json
